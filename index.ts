import * as Vector from './src/vec.ts'
import * as Matrix from './src/matrix.ts'
import * as Triangle from './src/triangle.ts'
import { Vec3, Mat, Vec } from './src/types.ts'

// const n: Vec3 = [1, 1, 1]
// const scaleMatrix = Matrix.createScaleMatrix(n, 0)
// const projectionMartix = Matrix.createOrthogonalProjectionMatrix(n)
// const point = [10, 20, 10]
// console.log(Matrix.multi(projectionMartix, point))
// console.log(Matrix.multi(scaleMatrix, point))
